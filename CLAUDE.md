# Restart at the repo root

**If this session started in this folder, stop and tell the operator to restart Claude Code at the repo root** (two folders up). The permission rules, hooks and project agent load only from the root `.claude/`, so a session started here runs without them. Rules: the root's `CLAUDE.md`.
